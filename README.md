## Empaquetar

```
npm install
npm run build
```

El comando usa `@yao-pkg/pkg` (compatible con Node 20) e incluye `yt-dlp.exe` y `ffmpeg.exe` dentro del ejecutable.
El `.exe` resultante se genera en la misma carpeta del proyecto.